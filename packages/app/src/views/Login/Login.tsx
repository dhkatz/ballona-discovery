import { Container } from 'react-bootstrap';
import { Authentication } from './Authentication';

export const Login = () => {
	return (
		<Container>
			<h1>Login</h1>
			<Authentication />
		</Container>
	);
};
